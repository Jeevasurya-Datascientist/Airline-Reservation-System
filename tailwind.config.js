// tailwind.config.js
module.exports = {
  content: [
    './/*.{html,php,js}',
  ],
  theme: {
    extend: {
      colors: {
        // Custom color palette if needed
      },
      animation: {
        // Add custom animations if needed
      },
      keyframes: {
        // Add custom keyframes if needed
      },
    },
  },
  plugins: [],
}